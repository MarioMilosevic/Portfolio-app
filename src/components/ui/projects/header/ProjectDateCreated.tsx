
const ProjectDateCreated = ({children}:{children:React.ReactNode}) => {
  return (
    <span className="text-sm">
      Date of creation: {children}
    </span>
  )
}

export default ProjectDateCreated
